<UserControl x:Class="Telbot.Items.ItemGroup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Height="50" Background="Cornsilk"
             Loaded="UserControl_Loaded">
    <Grid HorizontalAlignment="Stretch">
        <TextBlock x:Name="txt_title" HorizontalAlignment="Center">group ها</TextBlock>
        <Grid VerticalAlignment="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                
            </Grid>
            <Grid Grid.Column="1" >
                <Label x:Name="lbl_username" HorizontalAlignment="Right"  FlowDirection="LeftToRight">@bestgroups</Label>
            </Grid>
        </Grid>

    </Grid>
</UserControl>
